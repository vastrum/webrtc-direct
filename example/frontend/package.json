{
  "name": "webrtc-direct-echo-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "(cd ../client-wasm && CARGO_TARGET_DIR=../../target-wasm wasm-pack build --dev --target bundler) && vite build"
  },
  "dependencies": {
    "vite-plugin-wasm": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.2.2",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
